---
galaxy_info:
  role_name: azure_public_ip
  author: ansible-automation-platform
  description: Creates an optional Azure Public IP Address
  license: Apache-2.0
  min_ansible_version: "2.14"
  platforms:
    - name: all
  galaxy_tags:
    - azure
    - cloud
    - networking

dependencies:
  - role: azure_resource_group
